(function($, cloudStack) {
  //
  // Put a list of all your plugins here,
  // in quotations and separated by a comma.
  //
  // --This should be named the sample as your plugin folder,
  //   so if your plugin is in /plugins/testPlugin, then add
  //   'testPlugin' to the end of the list
  //
  cloudStack.plugins = [
    'testPlugin',
    'clickHere'
  ];
}(jQuery, cloudStack));
